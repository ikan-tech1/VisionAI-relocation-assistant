# Infrastructure

Infrastructure placeholders for deployment and environment provisioning.

Suggested next steps:

1. Add deployment target files (e.g. Vercel, AWS ECS, GCP Cloud Run).
2. Add database migration workflow and managed Postgres provisioning.
3. Add object storage configuration for persisted scan frames.
